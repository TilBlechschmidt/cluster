platform: linux

image_resource:
  type: registry-image
  source:
    # TODO Replace with official flux image :)
    repository: flux.blechschmidt.dev/cdk8s
    username: flux
    password: ((registry))

inputs:
  - name: cdk8s
  - name: cluster

params:
  REGISTRY_TOKEN: ((registry))

run:
  path: cdk8s/ci/push
